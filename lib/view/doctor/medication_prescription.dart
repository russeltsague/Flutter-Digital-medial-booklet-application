import 'package:flutter/material.dart';

class MedicationPrescriptionScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Medication Prescription')),
      body: Center(child: Text('Medication Prescription Screen')),
    );
  }
}
